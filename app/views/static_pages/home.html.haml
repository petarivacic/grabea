- if signed_in?
  .row
    %aside.span4
      %section
        = render 'shared/user_info'
      %section
        = render 'shared/stats'
      %section
        = render 'shared/micropost_form'
    .span8
      %h3 Activity
      = render 'shared/feed'
- else
  - provide(:title, 'Home')
  .center.hero-unit
    %h1 Vork
    %h2 your #1 source for finding local gigs and part-time jobs
    = link_to "Check it out", new_user_registration_path, class: "btn btn-large btn-primary"







