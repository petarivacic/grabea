<div class="well">
<%= form_for(@micropost) do |f| %>
  <%= render 'shared/error_messages', object: f.object %>
  <div class="field">
    <%= f.text_area :content, placeholder: "Type in your message..." %>
  </div>
  <%= f.submit "Post", class: "btn btn-info" %>
<% end %>
</div>
