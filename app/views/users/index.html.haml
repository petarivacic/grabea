- provide(:title, 'All users')

%h1 All users
= will_paginate
%ul.users
  - @users.each do |user|
    %li
      = image_tag user.gravatar_url, style: "width: 50px; height: 50px;",  class: "img-rounded"
      = link_to user.full_name, user
= will_paginate




