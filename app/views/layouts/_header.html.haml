%header.navbar.navbar-fixed-top.navbar.navbar-inverse
  .navbar-inner
    .container
      = link_to "MOUSE.IO", root_url, id: "logo"
      %nav
        %ul.nav.pull-right
          %li= link_to "Explore", static_pages_about_path
          - if user_signed_in?
            %li#fat-menu.dropdown
              %a.dropdown-toggle{"data-toggle" => "dropdown", :href => "#"}
                = current_user.full_name
                %b.caret
              %ul.dropdown-menu
                %li= link_to "Classmates", users_path
                %li= link_to "My Profile", current_user
                %li= link_to "Account Settings", edit_user_registration_path(current_user)
                %li.divider
                %li= link_to "Sign out", destroy_user_session_path, :method => :delete
          - else
            %li= link_to "Sign in", new_user_session_path
            